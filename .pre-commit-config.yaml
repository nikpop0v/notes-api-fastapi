repos:
  - repo: local
    hooks:
      - id: ruff
        name: ruff (lint + fix)
        entry: ruff
        language: system
        types: [python]
        args: ["check", "--fix"]

      - id: black
        name: black (format)
        entry: black
        language: system
        types: [python]
